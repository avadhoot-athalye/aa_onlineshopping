<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/webflow
                           http://www.springframework.org/schema/webflow/spring-webflow-2.4.xsd">



<!-- Executed when webFlow is triggered --> 
	<on-start>
<!--  		CartHandler is java class available through annotated CartHandler.java  -->
<!-- 			class.here we are calling it’s initFlow() method. -->
		<evaluate expression="cartHandler.initFlow()" result="flowScope.orderBean" />
	</on-start>

 	<!-- if not specified the first view state is the starting state -->
	<view-state id="start" view="cart-webflow/confirmDetails" model="flowScope.orderBean">
		<transition on="edit" to="address" />
		<transition on="submit" to="start" />
	</view-state>
	
	<action-state id="confirmation" >
<!-- 		<evaluate expression="cartHandler.addUser(user)"></evaluate> -->
		<transition on="success" to="address" />
		<transition on="failure" to="start" />
	</action-state>
	
	<view-state id="address" view="confirm_address" >
<!-- 		<evaluate expression="userHandler.addUser(user)"></evaluate> -->
		<transition on="success" to="address" />
		<transition on="failure" to="start" />
	</view-state>
	
	<end-state id="welcome" view="welcome" />
	<end-state id="login" view="externalRedirect:contextRelative:login" />
	<end-state id="toException"
		view="externalRedirect:contextRelative:exception" />

 	<!-- these events are available in every state -->
	<global-transitions>
		<transition on="home" to="toHome" />
		<transition on="error" to="toException" />
	</global-transitions>
</flow>